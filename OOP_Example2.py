'''
Principle of OOP:

'''
# Abstart class = a class which contains one or more abstract methods
# Abstract method = a method that has a declaration but does not have an implementation.
# Encapsulatio = Restricting the direct access 
from abc import ABC, abstractmethod








